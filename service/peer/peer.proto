syntax = "proto3";

option go_package = "./peer_grpc";
package peer_grpc;

service Peer {
    rpc HealthCheck(HealthCheckRequest) returns (HealthCheckResponse) {}
}

// 请求消息
message HealthCheckRequest{
}

// 响应消息
message HealthCheckResponse {
    int64 BuildTime = 1;
    string RespositoryVersion = 2;
    int64 UpTime = 3;
    int64 ResourceBuildTime = 4;
    string ResourceRespositoryVersion = 5;
}


